import { EscuelaDigitalPipe } from './escuela-digital.pipe';

describe('EscuelaDigitalPipe', () => {
  it('create an instance', () => {
    const pipe = new EscuelaDigitalPipe();
    expect(pipe).toBeTruthy();
  });
});
